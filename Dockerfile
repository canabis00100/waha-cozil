FROM devlikeapro/waha:latest

# WAHA já vem configurado, não precisa de comandos adicionais
# As variáveis de ambiente serão configuradas no Render

# Expõe a porta padrão do WAHA
EXPOSE 3000

# O WAHA já tem um CMD definido na imagem base

